# 腳本使用說明

本目錄包含用於快速操作專案的各種腳本。所有腳本都可以從專案根目錄執行。

## 📁 腳本列表

| 腳本名稱 | 用途 | 說明 |
|----------|------|------|
| `start-electron.sh` | 🚀 啟動 Electron 版本 | 自動編譯並啟動應用程式 |
| `dev-electron.sh` | 🔧 開發模式 | 啟動 Vite dev server（需另開終端執行 Electron） |
| `build-electron.sh` | 🔨 僅編譯 | 編譯但不啟動，用於驗證程式碼 |
| `package-electron.sh` | 📦 打包安裝檔 | 產生可發佈的安裝程式 |
| `start-go.sh` | 🐹 啟動 Go 版本 | 編譯並啟動 Go 客戶端 |

---

## 🚀 快速開始

### 啟動 Electron 版本（推薦）

```bash
./scripts/start-electron.sh
```

這會自動：
1. 檢查並安裝 npm 依賴
2. 編譯 TypeScript 和前端資源
3. 啟動 Electron 應用程式

### 啟動 Go 版本

```bash
./scripts/start-go.sh
```

---

## 🔧 開發模式

如果您需要修改程式碼並即時看到變更：

### 步驟 1：啟動開發伺服器

```bash
./scripts/dev-electron.sh
```

等待看到以下訊息：
```
➜  Local:   http://localhost:5173/
```

### 步驟 2：在另一個終端機啟動 Electron

```bash
cd electron-client
npm start
```

> **💡 提示**：開發模式下，修改 `src/renderer/` 的程式碼會自動熱更新。  
> 修改 `src/main/` 或 `src/preload/` 則需要重新啟動 Electron。

---

## 🔨 編譯

僅編譯程式碼（不啟動）：

```bash
./scripts/build-electron.sh
```

編譯輸出目錄：
- `electron-client/dist/main/` - 主程序
- `electron-client/dist/preload/` - 預載腳本
- `electron-client/dist/renderer/` - 渲染程序（前端）

---

## 📦 打包發佈

建立可安裝的應用程式：

```bash
./scripts/package-electron.sh
```

打包完成後，安裝檔案位於 `electron-client/release/` 目錄：

| 平台 | 檔案格式 |
|------|----------|
| Windows | `.exe` (安裝程式) |
| macOS | `.dmg` (磁碟映像) |
| Linux | `.AppImage`, `.deb`, `.rpm` |

---

## ⚠️ 常見問題

### Q: 執行腳本時出現 "Permission denied"

**A:** 給予腳本執行權限：
```bash
chmod +x scripts/*.sh
```

### Q: npm 指令找不到

**A:** 請確保已安裝 Node.js (建議 v18 以上)：
```bash
node --version
npm --version
```

### Q: Go 指令找不到

**A:** 請確保已安裝 Go (建議 v1.21 以上)：
```bash
go version
```

---

## 📝 備註

- 所有腳本都使用 `set -e`，遇到錯誤會立即停止執行。
- 腳本會自動切換到正確的工作目錄，您可以從專案任何位置執行它們。
- 首次執行時會自動安裝 npm 依賴，可能需要幾分鐘時間。
